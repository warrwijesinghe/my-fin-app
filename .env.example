# Never commit the real .env file.
DATABASE_URL="mysql://DB_USER:DB_PASSWORD@DB_HOST:3306/DB_NAME"
FIN_APP_PASSWORD="change-this-before-first-deploy"
FIN_SESSION_SECRET="generate-a-long-random-value-before-first-deploy"

# Set using the credential setup script; never commit a real password.
FIN_WIFE_PASSWORD_HASH=
