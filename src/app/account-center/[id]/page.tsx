import Link from "next/link";import { notFound } from "next/navigation";import { Nav } from "@/components/nav";import { AccountCenter } from "@/components/account-center";import { requireSession } from "@/lib/auth";import { rows } from "@/lib/db";import type { AccountBalance } from "@/lib/types";import type { AccountEntry } from "@/lib/account-center";import type { RowDataPacket } from "mysql2";import "../account-center.css";import "../account-balance-colors.css";
export const dynamic="force-dynamic";
export default async function Statement({params}:{params:Promise<{id:string}>}){await requireSession();const {id}=await params;const accountRows=await rows<RowDataPacket & AccountBalance>("SELECT a.owner,a.id,a.name,a.type,a.scope,a.holder,a.creditLimit,a.includeInAvailable,a.isActive,COALESCE(SUM(e.amount),0) balance FROM Account a LEFT JOIN AccountEntry e ON e.accountId=a.id WHERE 1=1 GROUP BY a.id ORDER BY a.isActive DESC,a.scope,a.name");const accounts=accountRows.map(a=>({...a,balance:Number(a.balance),creditLimit:a.creditLimit==null?null:Number(a.creditLimit),isActive:Boolean(a.isActive),includeInAvailable:Boolean(a.includeInAvailable)}));if(!accounts.some(a=>a.id===id))notFound();const entries=await rows<RowDataPacket & AccountEntry>("SELECT e.id,e.transactionId,e.entryDate,e.createdAt,e.amount,t.type,t.status,t.description,t.counterparty,t.scope,c.name category,p.name project,k.name task,a.name source,d.name destination FROM AccountEntry e JOIN FinancialTransaction t ON t.id=e.transactionId LEFT JOIN Category c ON c.id=t.categoryId LEFT JOIN Project p ON p.id=t.projectId LEFT JOIN Task k ON k.id=t.taskId LEFT JOIN Account a ON a.id=t.accountId LEFT JOIN Account d ON d.id=t.destinationAccountId WHERE e.accountId=? ORDER BY e.entryDate,e.createdAt,e.id",[id]);return <><Nav/><main className="ac-page"><Link className="button" href="/account-center">← All accounts</Link><AccountCenter key={id} accounts={accounts} selectedId={id} entries={entries.map(e=>({...e,amount:Number(e.amount),entryDate:String(e.entryDate).slice(0,10),createdAt:String(e.createdAt)}))} statementView/></main></>}
