import crypto from "node:crypto";
import { NextResponse } from "next/server";
import { z } from "zod";
import { requireApiSession } from "@/lib/route-auth";
import { transaction } from "@/lib/db";
import { ACCOUNT_TYPES, SCOPES } from "@/lib/types";
const schema=z.object({name:z.string().trim().min(2).max(120),type:z.enum(ACCOUNT_TYPES),scope:z.enum(SCOPES),holder:z.string().trim().max(80).optional(),creditLimit:z.coerce.number().min(0).optional(),openingBalance:z.coerce.number().min(0),openingDate:z.string().optional(),includeInAvailable:z.boolean()});
export async function POST(request:Request){const denied=await requireApiSession();if(denied)return denied;const f=await request.formData(),p=schema.safeParse({name:f.get("name"),type:f.get("type"),scope:f.get("scope"),holder:f.get("holder")||undefined,creditLimit:f.get("creditLimit")||undefined,openingBalance:f.get("openingBalance")||0,openingDate:f.get("openingDate")||undefined,includeInAvailable:f.get("includeInAvailable")==="on"});if(!p.success)return NextResponse.redirect(new URL("/accounts?error=invalid",request.url));const d=p.data,date=d.openingDate||new Date().toISOString().slice(0,10),accountId=crypto.randomUUID();await transaction(async c=>{await c.execute("INSERT INTO `Account` (id,name,type,scope,holder,creditLimit,includeInAvailable,openingDate,updatedAt) VALUES (?,?,?,?,?,?,?,?,NOW(3))",[accountId,d.name,d.type,d.scope,d.holder||null,d.type==="CREDIT_CARD"?d.creditLimit??null:null,d.includeInAvailable,date]);if(d.openingBalance){const tx=crypto.randomUUID();await c.execute("INSERT INTO `FinancialTransaction` (id,type,status,amount,transactionDate,description,scope,accountId,updatedAt) VALUES (?,'OPENING_BALANCE','POSTED',?,?, 'Opening balance',?,?,NOW(3))",[tx,d.openingBalance,date,d.scope,accountId]);await c.execute("INSERT INTO `AccountEntry` (id,accountId,transactionId,amount,entryDate) VALUES (?,?,?,?,?)",[crypto.randomUUID(),accountId,tx,d.openingBalance,date]);}});return NextResponse.redirect(new URL("/accounts?created=1",request.url));}
