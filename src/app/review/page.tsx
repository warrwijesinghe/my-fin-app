import { Nav } from "@/components/nav"; import { requireSession } from "@/lib/auth"; import { rows } from "@/lib/db"; import { lkr,dateValue } from "@/lib/format";
export const dynamic="force-dynamic";
export default async function Page(){await requireSession();const [items,accounts]=await Promise.all([rows<any>("SELECT * FROM `FinancialTransaction` WHERE status='PENDING_REVIEW' ORDER BY createdAt"),rows<any>("SELECT * FROM `Account` WHERE isActive=1 ORDER BY name")]);return <><Nav/><main><div className="page-heading"><div><p className="eyebrow">Control point</p><h1>Review incomplete entries</h1><p className="muted">Balances update only after you select a payment account.</p></div></div><section className="panel">{items.length?<div className="review-list">{items.map(i=><form className="review-item" action={`/api/review/${i.id}`} method="post" key={i.id}><div><strong>{i.type.replace("_"," ")} · {lkr(i.amount)}</strong><span>{dateValue(new Date(i.transactionDate))} · {i.description||"No description"}</span></div><select name="accountId" defaultValue=""><option value="" disabled>Select account</option>{accounts.map(a=><option key={a.id} value={a.id}>{a.name}</option>)}</select><button className="button primary">Confirm</button></form>)}</div>:<div className="empty"><p>Everything is reviewed.</p></div>}</section></main></>}
